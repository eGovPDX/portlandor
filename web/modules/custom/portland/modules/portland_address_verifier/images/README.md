This folder holds image assets used by the Address Verifier map UI.

Expected files (copied from portland_location_picker or created to match):
- map_aerial.png: icon for toggling to aerial basemap.
- map_base.png: icon for toggling back to base basemap.

Place PNGs at the paths above. If these files are missing, the aerial toggle control will still function but display no icon.
