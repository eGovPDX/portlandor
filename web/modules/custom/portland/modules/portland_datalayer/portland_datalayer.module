<?php

/**
 * Implements hook_datalayer_alter()
 */
function portland_datalayer_datalayer_alter(&$data_layer) {
  // // Modify data layer for user roles
  // if (isset($data_layer["userRoles"])) {
  //   foreach ($data_layer["userRoles"] as $user_role) {
  //     $data_layer['userRole'] = $user_role;
  //   }
  // }
}
