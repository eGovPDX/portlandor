services:
  portland_webform_rules.rules_sync:
    class: Drupal\portland_webform_rules\Service\RulesSyncService
    arguments:
      - '@http_client'
      - '@secrets'
      - '@entity_type.manager'
    tags: [container.service_subscriber]
