services:
  mem_migrate_oembed.migrate:
    class: Drupal\mem_migrate_oembed\MemMigrate
    arguments: ['@config.factory', '@entity.last_installed_schema.repository', '@database', '@keyvalue', '@entity_field.manager']
