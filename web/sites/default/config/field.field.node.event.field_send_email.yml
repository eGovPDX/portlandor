uuid: 8e908ec2-3f8d-471e-b81b-93898d58afec
langcode: en
status: true
dependencies:
  config:
    - field.storage.node.field_send_email
    - node.type.event
  module:
    - tmgmt_content
third_party_settings:
  tmgmt_content:
    excluded: false
id: node.event.field_send_email
field_name: field_send_email
entity_type: node
bundle: event
label: 'Send email via GovDelivery to topic subscribers'
description: 'Selecting this option will send an email notification to all subscribers of the selected topic(s) above. The email will include the title, summary, and a link to this item. Emails will only be sent for published content or upon scheduled publishing. Emails will be delivered to users according to their individual subscription preferences (either immediately or in a combined daily or weekly digest). The checkbox will reset automatically once an email is sent and must be selected again if you update the content and wish to send another email to subscribers.'
required: false
translatable: false
default_value: {  }
default_value_callback: ''
settings:
  on_label: 'On'
  off_label: 'Off'
field_type: boolean
