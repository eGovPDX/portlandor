display:
  default:
    display_options:
      exposed_form:
        options:
          submit_button: 过滤
      fields:
        path:
          label: 路径
        count:
          label: 计数
        langcode:
          label: 语言
        redirect_404_operations:
          label: 操作
      filters:
        path:
          expose:
            label: 路径
        langcode:
          expose:
            label: 语言
