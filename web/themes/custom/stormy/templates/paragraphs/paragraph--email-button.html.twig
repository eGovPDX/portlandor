{% extends 'paragraph.html.twig' %}
{% block content %}
    <a class="btn btn-primary btn--email" href="{{ content.field_email_link[0]['#url'].toString() }}" role="button">
        {{ content.field_email_text[0]['#context']['value']|lower }}
    </a>
{% endblock %}
