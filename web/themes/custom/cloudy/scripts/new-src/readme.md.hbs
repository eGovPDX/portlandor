# {{ sentenceCase name }}

## Props

- `text`*: string - Required text that is placed inside
- `type`: primary (default), secondary - Different types possible
- `isDark`: boolean

## Usage

```twig
{% include "@{{ type }}/{{ dashCase name }}/{{ dashCase name }}.twig" with {
  "text": "Hello World!",
} only %}
```
