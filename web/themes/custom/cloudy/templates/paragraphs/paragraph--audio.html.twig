{% extends 'paragraph.html.twig' %}
{% block content %}
{% if content.field_video.0 %}
  <figure class="figure">
    {% if (not content.field_video.0['#media'].isPublished()) %}
      <span class="badge badge-danger audio-unpublished">Unpublished</span>
    {% endif %}
    {{ content }}
    <figcaption class="figure-caption text-center">
      {{ paragraph.field_caption.value }}
    </figcaption>
  </figure>
{% endif %}
{% endblock %}