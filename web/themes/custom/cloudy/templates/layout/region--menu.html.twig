{% set classes = classes|default([
    'sidebar-left',
    'col-12',
    'col-lg-3'
  ])
%}

{% if content %}
  {% spaceless %}
    <div {{ attributes.addClass(classes) }}>
      {{ content }}
    </div>
  {% endspaceless %}
{% endif %}
