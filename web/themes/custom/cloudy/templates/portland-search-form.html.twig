{% set classes = [
  'input-group',
  size == 'big' ? 'input-group-lg mt-5 mb-5' : '',
  size == 'small' ? 'input-group-sm' : '',
] %}

<div{{ attributes.addClass(classes) }}>
  {{ input }}
  <div class="input-group-append">
    {{ buttons }}
  </div>
</div>
