<div class="teaser d-flex flex-column align-items-start mb-5">
  <h3>
    <a href="{{ url }}" rel="bookmark">
      {{ label }}</a>
  </h3>
  {{ content.field_popular }}
  {% if node.field_summary.value %}
    <div>
      <p>{{ node.field_summary.value }}</p>
    </div>
  {% endif %}
</div>
