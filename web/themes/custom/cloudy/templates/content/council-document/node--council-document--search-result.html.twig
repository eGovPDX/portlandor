{% if content.field_is_emergency_ordinance[0]['#markup'] == 'true' %}
  {% set document_type = content.field_is_emergency_ordinance %}
{% else %}
  {% set document_type = content.field_council_document_type %}
{% endif %}

{% include '@components/morsel/morsel.twig' with {
  is_draft: node.isPublished() ? false : true,
  layout_mt: 8,
  layout_mb: 8,
  heading: {
    url: url,
    text: label,
    level: 2,
    style_override: 3
  },
  text: content.field_full_document_title,
  type: document_type
} %}
