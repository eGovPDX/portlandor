{{ attach_library('classy/node') }}
{% include "@components/morsel/morsel.twig" with {
  is_draft: node.isPublished() ? FALSE : TRUE,
  heading: {
    text: label,
    level: 2,
  },
  url: url,
  text: content.field_summary.0,
  type: content.field_park_location_type.0,
  location: content.field_location.0,
} only %}
