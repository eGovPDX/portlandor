{# Template for Featured News Block #}
<hr class="separator mb-8 mt-0">
<div class="featured row mb-10">
    <div class="col-12 col-lg-4">
        {% if label %}
            <h2 class="mb-8">{{ label }}</h2>
        {% endif %}
    </div>
    <div class="col-12 col-lg-8 p-0">
        {{ content }}
    </div>
</div>
