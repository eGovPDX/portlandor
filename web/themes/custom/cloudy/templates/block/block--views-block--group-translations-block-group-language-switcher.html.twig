{# Only display block if there's more than one langauge for the page #}
{% if content['#view'].result|length > 1 %}
  {% include "block.html.twig" with {
    title_classes: ['h5 mt-4 mb-1']
  } %}
{% endif %}
