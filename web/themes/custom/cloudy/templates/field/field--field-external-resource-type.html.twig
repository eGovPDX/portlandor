{% set modes = {
  full: 4
} %}
{% set mb = modes[view_mode] ? modes[view_mode] : null %}

{% if view_mode != 'featured' %}
  {% include '@components/badge/badge.twig' with {
    mb: mb,
    text: element[0]
  } only %}
{% else %}
  {% include 'field.html.twig' %}
{% endif %}
