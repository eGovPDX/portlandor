{% for item in items %}
<div class="rounded-circle overflow-hidden">
  {# Mark as decorative image by setting alt attribute to empty string #}
  {{ item.content|set_attribute('alt', '') }}
</div>
{% endfor %}
