{% set mb = mb[view_mode] != full or related ? 2 %}
{% set badge_color = 'red' %}

{% if element[0]['#markup'] == 'true' %}
  {% include '@components/badge/badge.twig' with {
    mb: mb,
    text: 'emergency closure'|t|title,
    color: badge_color,
    icon: {
      name: 'warning',
      size: 'xs'
    }
  } %}
{% endif %}
