{% set modes = {
  full: 4
} %}
{% set mb = modes[view_mode] ? modes[view_mode] : 2 %}

{% include '@components/badge/badge.twig' with {
  mb: mb,
  text: ('city code ' ~ (element[0]|render))|t|title
} only %}
