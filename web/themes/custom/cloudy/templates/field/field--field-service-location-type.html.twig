{% set modes = {
  full: 4,
  related: 2,
  teaser: 2
} %}
{% set mb = modes[view_mode] ? modes[view_mode] %}

{% include '@components/badge/badge.twig' with {
  mb: mb,
  text: element[0]|render|title
} only %}
